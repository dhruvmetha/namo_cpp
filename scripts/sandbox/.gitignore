# Quarantine for one-off / disposable scripts: diagnostics, experiments,
# point-in-time cleanups. NOT committed (that's the point — see README.md).
# Durable pipeline/eval tooling belongs in scripts/, not here.
*
!.gitignore
!README.md
# durable: _full_search eval/plot tooling (curves + a/b/c/d), referenced by the card
!aggregate_fullsearch.py
