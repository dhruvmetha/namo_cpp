# Full-NAMO aligned integration: authorized steps 1–7

The user authorized preparing and validating an isolated Amarel build aligned with dhruv-linux. Stop after cross-machine fixed-action replay. Preserve the existing campaign and all live source trees. Do not submit benchmark arrays, reclassify the 1,000 scenes, retrain, cancel old jobs, or promote this branch.

1. Save the existing campaign's source, build, config, artifact hashes, completed result hashes, and Slurm state. Existing jobs can continue appending their own results.
2. Pin dhruv-linux source `0920eee91ed4a9ec9f4a1183c0eca67019193d9d`, Python 3.12.13, and the actually linked MuJoCo source `dbe18f574006adbc410f1c0551a53a24e6156c6e` (reports 3.2.0). Record the Python dependency manifest separately from the linked physics library; the pip MuJoCo package is 3.8.1.
3. Use branch `integration/full-namo-amarel-aligned-20260911` in the dhruv-linux linked worktree `.worktrees/full-namo-amarel-aligned-20260911`, and Amarel root `/scratch/tdn39/full_namo_dhruv_aligned_20260911_v1`. Keep separate source, environment, artifacts, logs, and results there. Rebuild native NAMO and MuJoCo on Amarel; do not transfer dhruv's NAMO binding.
4. Port only the existing Full-NAMO timing integration from Amarel commit `3cd13decc92f7c7ed3f897a75d17b6947e9ed8cc`; preserve dhruv's joint-blocker graph, pooled boundary search, goal clearance, and hmax=2 semantics. Main agent owns focused commits; subagents never commit.
5. Build the aligned native libraries on an Amarel compute node, with NAMO targeting x86-64-v3. Record source hashes, compiler flags, headers, imported module path, actual linked library, version functions, and binary hashes. Snapshot configs, primitives, checkpoints and Sage code; use explicit 1 mm campaign config for physics checks.
6. Validate single-blocker parity, pooled A→B behavior (including B becoming reachable after A), goal clearance, authoritative success verification, and cumulative simulator budget and timing over multiple openings. Reconcile the golden fixture's 5 mm versus 1 mm sidecar and sampled point portability separately from planner changes. Replay and diagnose the saved `same_region_but_goal_unreachable` case without counting graph connectivity as success.
7. Replay a bounded fixed set of pushes on both machines with identical input hashes and pinned code/config. Compare states, region/reachability information, and search decisions. Record numerical differences and explain any behavioral divergence. Do not compare wall times across machines.

Acceptance is evidence per step, not merely a successful build. Bounded validation failures must be investigated and reported; do not broaden to a full benchmark to hide them. Unit tests may run before commits to establish red/green behavior. Commit the protocol and relevant code before physics validation runs, as required by the repository experiment workflow.

The local directory `full_namo_aligned_stage_20260911` stores transfer files and durable provenance copies. Removing this isolated work after review requires only deleting its Amarel root, its output directory, and removing the linked dhruv worktree/branch; no production changes or job rollback should be necessary.
