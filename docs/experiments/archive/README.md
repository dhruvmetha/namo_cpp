---
status: archive
tags: [archive]
updated: 2026-07-02
---
# Archive — finished experiments & closed snapshots

Notes here are **done**: finished experiment cards (moved from [../log/](../log/) when they reach
`status: done`) and closed dead-end snapshots that no active work links. They stay in git and on the Bases
board (the vault-wide `type: experiment` query), but are out of the active `experiments/` view. Their
headline results live in [../RESULTS.md](../RESULTS.md). Do not cite as current.

> Rule: a doc is archived when it is **finished AND not linked by active (live) work.** Frozen journals that
> live journals still cite as evidence stay in `experiments/` (already hidden from the live board by status).
