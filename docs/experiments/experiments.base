filters:
  and:
    - type == "experiment"
views:
  - type: table
    name: Experiments
    groupBy:
      property: note.status
      direction: ASC
    order:
      - file.name
      - status
      - created
      - metric
      - commit
    limit: 100
